@model IEnumerable<DoctorSkin.Models.Brands>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.namebrand)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.hidebrand)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.namebrand)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.hidebrand)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.idbrand }) |
                @Html.ActionLink("Details", "Details", new { id=item.idbrand }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.idbrand })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
